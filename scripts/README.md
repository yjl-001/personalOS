# Scripts Map

Reusable operational entrypoints live here.

- `flowus_mcp_smoke.rb`: validates FlowUS MCP access, schema, and end-to-end writes
- `weread_flowus_pipeline.js`: runs the safest known WeRead -> FlowUS ingest path

Prefer these scripts over ad hoc shell snippets when the task matches them.
